# Flight Search Frontend

React frontend برای جستجوی پرواز

## نصب

```bash
npm install
```

## اجرا

```bash
npm run dev
```

سایت در آدرس `http://localhost:3000` باز می‌شود.

## تنظیمات

فایل `.env` بسازید:

```
VITE_API_URL=http://localhost:5000/api
```

